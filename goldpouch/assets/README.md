# Assets

This folder is reserved for website media assets.

- `assets/images/` — store image files used by the site.
- `assets/icons/` — store custom icons, SVGs, or icon sets.
- `assets/fonts/` — store font files used by the site.

When adding assets, update `index.html` or `styles/styles.css` to reference the correct paths.
